const assert = require("assert");


describe("[SKIPALL] Array should have 4 elements", () => {
    before(function() {
        this.skip();
    });
    
    it("should have 4 elements", () => {
        assert.ok(true);
    });

    it("should have 4 elements", () => {
        assert.ok(true);
    });
});